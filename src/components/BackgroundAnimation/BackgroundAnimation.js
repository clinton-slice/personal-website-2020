import React from "react";
import classNames from "classnames";
import { Sketch } from "./styledComponent";
const BackgroundAnimation = () => {
  return <Sketch></Sketch>;
};

export default BackgroundAnimation;
